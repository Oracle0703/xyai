# Sub2API v0.1.178 Upstream Merge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task with verification checkpoints.

**Goal:** 从本地最新 `main` 创建 `feature/hy/10178_merge_sub2api_178`，固定合并 `Wei-Shaw/sub2api` `main@49504adc98d2b6d539491e865a340e644548979e`，只解决冲突并保留未重叠的本地 features。

**Architecture:** 先锁定本地 `main` 与上游 SHA 的版本和祖先边界，再以 `--no-commit --no-ff` 进入审查态。逐文件审查文本冲突及双方修改路径；重叠功能采用上游实现，唯一的本地功能保留；不修复上游自身 bug。最后更新 append-only merge ledger 与相关 llm-wiki，完成测试并停在 `MERGE_HEAD` 等待用户审核。

**Tech Stack:** Git merge/tree inspection, Go backend, Vue 3/Vite frontend, repository llm-wiki.

## Global Constraints

- 基线必须是本地最新 `main`，分支命名遵循 `feature/hy/10xxx_XXX`。
- 上游边界固定为 `Wei-Shaw/sub2api main@49504adc98d2b6d539491e865a340e644548979e`。
- 只解决冲突；上游引入的 bug 记录但不在本分支修复。
- 功能重叠时采用上游实现；唯一的本地 `features` 功能继续保留。
- 更新 `docs/features/sub2api -merage-list.md` 与受影响的 `llm-wiki/wiki/*.md`，保持 append-only/LF。
- 用户要求在 commit 前停止；不创建 commit、push、PR、merge 或部署。

---

### Task 1: Lock refs and create review branch

**Files:**
- Create: `docs/superpowers/plans/2026-08-19-sub2api-v0.1.178-sync.md`

- [ ] Fetch `github/main` and `upstream/main`, verify exact upstream object and `backend/cmd/server/VERSION`.
- [ ] Fast-forward local `main` only if remote `github/main` is newer and the worktree remains clean.
- [ ] Create and switch to `feature/hy/10178_merge_sub2api_178` from the locked local `main`.
- [ ] Record base SHA, target SHA, merge base, version evidence, and branch state.

### Task 2: Preflight and execute the no-commit merge

**Files:**
- Modify: files reported by `git merge-tree` / `git merge --no-commit --no-ff`.

- [ ] Run `git merge-tree --write-tree --messages main <target>` and classify textual conflicts plus likely semantic-overlap files.
- [ ] Run `git merge --no-commit --no-ff <target>`.
- [ ] Resolve each conflict from three-way content, preserving upstream behavior for overlaps and local-only feature paths.
- [ ] Regenerate Wire/Ent outputs only when source changes require it; do not make unrelated cleanup changes.
- [ ] Verify no unmerged index entries, conflict markers, or accidental deletions under `docs/features`.

### Task 3: Update durable merge documentation

**Files:**
- Modify: `docs/features/sub2api -merage-list.md`
- Modify: relevant `llm-wiki/wiki/*.md`

- [ ] Append the merge date, branch, upstream branch/SHA, base, pending merge state, conflict files and semantic handling.
- [ ] Record known upstream issues without fixing them locally.
- [ ] Update version/architecture facts in backend/frontend/ops/domain/security wiki pages only where the source changed.
- [ ] Stage ignored docs with `git add -f` when required.

### Task 4: Verify and hand off at commit gate

**Files:**
- Verify staged merge snapshot and repository state.

- [ ] Run `git diff --cached --check`, `git ls-files -u`, conflict-marker scan, and ancestry checks.
- [ ] Run focused and repository-appropriate backend/frontend tests using repo-local Windows Go caches and fresh `GOTMPDIR`.
- [ ] Recheck `git status --short --branch`, `MERGE_HEAD`, exact upstream SHA, docs entries, and no push/commit side effects.
- [ ] Stop and notify the user for review before any commit is created.
