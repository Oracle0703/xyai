# sub2api v0.1.156 Upstream Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于本地 `main@4c456aad3` 合并 `Wei-Shaw/sub2api main` 的固定 0.1.156 目标 `d515c3045ce838...`，保留非重叠本地功能并在 commit 前完成文档与验证。

**Architecture:** 使用 `--no-commit --no-ff` 建立双父 merge 状态，以 merge-base/ours/theirs 审查文本冲突和自动合并文件。`docs/features` 作为本地功能目录，源码与测试作为最终事实；上游存在完整等价实现时收敛到上游版本。

**Tech Stack:** Git、Go 1.26.5、Gin、Ent、Redis、Vue 3、TypeScript、pnpm、Vitest。

---

### Task 1: 固定合并边界与基线

**Files:**
- Inspect: Git refs、`backend/cmd/server/VERSION`

- [x] **Step 1: 从本地 main 创建工作分支**

```powershell
git switch -c "feature/hy/10157_同步sub2api主线" main
```

Expected: `HEAD` 为新分支，起点为 `4c456aad3`。

- [x] **Step 2: 更新上游并验证固定目标**

```powershell
git fetch --prune upstream main --tags
git merge-base --is-ancestor "v0.1.156^{}" upstream/main
git show -s --format=%H "v0.1.156^{}"
git rev-list --parents -n 1 d515c3045ce838976ebedab87846aaaf893dbbf6
git show d515c3045ce838976ebedab87846aaaf893dbbf6:backend/cmd/server/VERSION
```

Expected: 标签目标为 `12f991d`；最终 main 边界 `d515c3045` 的唯一父为标签目标，且 `VERSION` 为 `0.1.156`。

### Task 2: 建立无提交合并并解决冲突

**Files:**
- Modify: Git 报告的全部冲突文件
- Test: 与冲突模块相邻的现有测试；若需手工改变行为，先补能够复现差异的测试

- [x] **Step 1: 记录合并前双方增量和重叠面**

```powershell
git diff --name-status "7c717365ef728e53cdcf6d639a4dd68226db03b2..d515c3045ce838976ebedab87846aaaf893dbbf6"
git diff --name-status "upstream/main...main"
git log --left-right --cherry-pick --oneline "main...d515c3045ce838976ebedab87846aaaf893dbbf6"
```

Expected: 形成上游增量、本地扩展和双方同时修改文件清单。

- [x] **Step 2: 合入固定目标但不提交**

```powershell
git merge --no-commit --no-ff d515c3045ce838976ebedab87846aaaf893dbbf6
```

Expected: 自动停止在待提交状态或进入可审查冲突状态，`MERGE_HEAD` 固定为目标 SHA。

- [x] **Step 3: 三方审查并解决每个冲突**

```powershell
git ls-files -u
git diff --check
rg -n "^(<<<<<<< |>>>>>>> |=======$)" -g "!*lock*"
```

Expected: 解决后 `git ls-files -u` 和冲突标记扫描均无输出。

### Task 3: 审查本地功能保留与上游重叠

**Files:**
- Inspect: `docs/features/**`
- Modify: 仅语义冲突或重复实现涉及的代码和测试

- [x] **Step 1: 建立功能到代码/测试映射**

```powershell
rg -n "RequestArchive|RequestIntercept|Prompt Metrics|Prompt Risk|Token Analysis|organization usage|image gen|concurrency" docs/features llm-wiki/wiki backend frontend
```

Expected: 每项本地能力标记为“保留、组合、由上游替代”之一。

- [x] **Step 2: 审查所有双方修改文件**

```powershell
git diff --name-only (git merge-base main d515c3045ce838976ebedab87846aaaf893dbbf6)..main
git diff --name-only (git merge-base main d515c3045ce838976ebedab87846aaaf893dbbf6)..d515c3045ce838976ebedab87846aaaf893dbbf6
```

Expected: 交集文件逐项审查，并以调用链和测试证明组合语义。

### Task 4: 文档收口

**Files:**
- Modify: `llm-wiki/wiki/README.md`
- Modify: `llm-wiki/wiki/backend.md`
- Modify: `llm-wiki/wiki/frontend.md`
- Modify: `llm-wiki/wiki/ops.md`
- Modify: `llm-wiki/wiki/data-and-domain.md`
- Modify: `llm-wiki/wiki/security-and-reliability.md`
- Modify: `docs/features/sub2api -merage-list.md`
- Create: `docs/reviews/2026-07-16-sub2api-v0.1.156-merge-review.md`
- Modify: `docs/delivery/2026-07-16-sub2api-v0.1.156-sync/*`

- [x] **Step 1: 只记录实际改变的稳定知识**

Expected: wiki 与源码一致，路径使用仓库相对路径，LF 换行，无敏感信息。

- [x] **Step 2: 追加 ledger 条目**

Expected: 仅尾部新增，记录日期、分支、上游 SHA、冲突、解决方式、验证结果；merge commit 在用户批准并创建后补录。

### Task 5: 审查与完整验证

**Files:**
- Modify: `docs/delivery/2026-07-16-sub2api-v0.1.156-sync/test-review.md`
- Modify: `docs/delivery/2026-07-16-sub2api-v0.1.156-sync/delivery-report.md`

- [x] **Step 1: 后端测试与静态检查**

```powershell
Push-Location backend
go test -tags=unit -p 1 -count=1 ./...
go test -tags=integration -p 1 -count=1 ./...
golangci-lint run ./...
golangci-lint run --new-from-rev main ./...
Pop-Location
```

Expected: 明确在 `backend` 工作目录执行；Go 测试使用 repo-local cache 和每轮 fresh `GOTMPDIR`，真实测试失败为 0。全量 lint 与合并差异 lint 均记录真实结果；如有既有债务，必须定位到基线，不得伪报通过。

Actual: unit 完整重跑退出 0；integration 完整跑到结尾但一个与 unit 文件集合相同的包被本机 360 拒绝启动；全量 lint 29 条均为 `main` 既有债务，`--new-from-rev main` 为 0 issues。环境限制与基线债务已进入交付报告。

- [x] **Step 2: 前端验证**

```powershell
cmd.exe /c pnpm --dir frontend run lint:check
cmd.exe /c pnpm --dir frontend run typecheck
cmd.exe /c pnpm --dir frontend exec vitest run
cmd.exe /c pnpm --dir frontend run build
```

Expected: lint、类型检查、全部测试和生产构建退出 0。

- [x] **Step 3: 在最新前端产物上执行 embed 构建**

```powershell
Push-Location backend
go build -tags embed -trimpath -o .gocache/sub2api-embed-final.exe ./cmd/server
Pop-Location
```

Expected: 必须在 `pnpm --dir frontend run build` 成功之后执行，确保嵌入的是本轮最新 `backend/internal/web/dist`。

- [x] **Step 4: Git 与范围硬门**

```powershell
git diff --check
git diff --cached --check
git diff HEAD --check
git ls-files -u
git rev-parse HEAD main MERGE_HEAD
git merge-base --is-ancestor eb2b8632ded614bf991d7d36abfa38b513ad8c2d MERGE_HEAD
git diff HEAD --diff-filter=D --name-only -- docs/features
rg -n "^(<<<<<<< |>>>>>>> |=======$)" -g "!*lock*"
git status --short --branch
```

Expected: `HEAD` 与本地 `main` 均为 `4c456aad3`，`MERGE_HEAD` 严格等于固定目标 `d515c3045`；后续上游 `eb2b8632d` 不是 `MERGE_HEAD` 的祖先（退出 1）；无 whitespace、unmerged、精确冲突标记或 `docs/features` 删除，并保持未提交 merge 状态等待审核。未提交 merge 期间不能用“目标是 `HEAD` 祖先”作为合入证明。

- [x] **Step 5: 通知用户审核，禁止提前提交**

Expected: 汇报冲突决策、功能映射、文件统计、测试结果与残余风险；不运行 `git commit`。
