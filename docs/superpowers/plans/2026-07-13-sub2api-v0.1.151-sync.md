# sub2api v0.1.151 主线同步实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `Wei-Shaw/sub2api` 最新 `main` 完整合入 `feature/hy/10151_同步sub2api主线`，同时保留本地扩展、补齐合并文档并通过全量验证。

**Architecture:** 以当前本地功能分支 `89711be23` 为合并基线，以刷新后的 `upstream/main` 为远端输入。所有冲突按 merge-base、ours、theirs 三方逐文件审查；冲突解决后继续审查高风险自动合并文件和本地能力入口，再更新追加型 merge ledger、merge review 与 `llm-wiki`。

**Tech Stack:** Git、Go 1.26.5、Gin/Ent、Vue 3、TypeScript、Vitest、pnpm。

---

### Task 1: 建立可复现基线

**Files:**
- Inspect: `AGENTS.md`
- Inspect: `llm-wiki/wiki/*.md`
- Inspect: `docs/features/sub2api -merage-list.md`

- [x] **Step 1: 确认隔离分支和工作区干净**

Run: `git status --short --branch && git branch --show-current`

Expected: 当前分支为 `feature/hy/10151_同步sub2api主线`，无用户未提交修改。

- [x] **Step 2: 安装前端依赖并准备 Go 本地缓存**

Run: `cmd.exe /c pnpm --dir frontend install --frozen-lockfile`

Expected: pnpm 退出码为 0，`pnpm-lock.yaml` 不发生变化。

- [x] **Step 3: 运行合并前基线验证**

Run: `go test -tags=unit -p 1 -count=1 ./...`（按 `llm-wiki/wiki/ops.md` 使用仓库内 `GOCACHE/GOMODCACHE` 与全新 `GOTMPDIR`）

Run: `cmd.exe /c pnpm --dir frontend run typecheck && cmd.exe /c pnpm --dir frontend exec vitest run`

Expected: 后端 unit、前端类型检查和 Vitest 均退出 0；若基线失败，先记录并停止合并。

### Task 2: 合并并逐项解决冲突

**Files:**
- Modify: Git 标记的全部冲突文件
- Inspect: `backend/internal/server/routes/`
- Inspect: `backend/internal/service/`
- Inspect: `frontend/src/router/`
- Inspect: `frontend/src/i18n/`

- [x] **Step 1: 固定上游提交并执行 merge**

Run: `git fetch upstream main --prune && git merge --no-ff upstream/main`

Expected: Git 创建 merge commit，或停在可审查的冲突状态。

- [x] **Step 2: 对每个冲突做三方审查**

Run: `git ls-files -u`，并对每个文件比较 `git show :1:path`、`:2:path`、`:3:path`。

Expected: 上游新增行为与本地 Prompt Risk、RequestArchive/Intercept、Token Analysis、图片生成、并发方案和 compatible cache usage 均被保留。

- [x] **Step 3: 检查自动合并的高风险文件**

Run: `git diff --cc`、`git diff HEAD^1..HEAD`、关键能力 `rg` 扫描与 focused tests。

Expected: 不存在 silent deletion、重复符号、路由中间件丢失、i18n key 缺失或 cache usage 覆盖回归。

### Task 3: 文档与验证收口

**Files:**
- Modify: `docs/features/sub2api -merage-list.md`
- Create: `docs/reviews/2026-07-13-upstream-<head>-merge-review.md`
- Modify: `llm-wiki/wiki/README.md`
- Modify: 按影响范围选择 `llm-wiki/wiki/backend.md`、`frontend.md`、`ops.md`、`data-and-domain.md`、`security-and-reliability.md`

- [x] **Step 1: 追加合并记录和 review**

Expected: 记录日期、工作分支、上游提交、merge commit、冲突文件、处理方式、能力保留和验证证据；已有历史内容只追加不覆盖。

- [ ] **Step 2: 运行全量验证**

Run: `go test -tags=unit -p 1 -count=1 ./...`

Run: `go test -tags=integration -p 1 -count=1 ./...`

Run: `cmd.exe /c pnpm --dir frontend run lint:check`

Run: `cmd.exe /c pnpm --dir frontend run typecheck`

Run: `cmd.exe /c pnpm --dir frontend exec vitest run`

Expected: 所有命令退出 0；另需通过 unresolved-file、冲突标记、`git diff --check` 检查。

- [ ] **Step 3: 收尾前再次刷新 upstream**

Run: `git fetch upstream main --prune && git rev-parse upstream/main`

Expected: 当前分支已经包含最终确认的 upstream HEAD；若上游继续前进，则继续增量 merge 并重跑受影响验证。
