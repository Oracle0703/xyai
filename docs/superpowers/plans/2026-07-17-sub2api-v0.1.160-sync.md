# sub2api v0.1.160 Upstream Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于本地 `main@5d5f157854b9a88cc57da1600095bb404b78ed45`，把 `Wei-Shaw/sub2api main@57914967cbb127ff715719c3879d881c10d75274` 的 `0.1.160` 基线合入 `feature/hy/10160_合并1.160版本`，保留非重叠本地功能，并在用户审核前保持未提交 merge 状态。

**Architecture:** 使用 `git merge --no-commit --no-ff` 保留双父拓扑，以 `c2c19a7cbe8486ebb5b56834d1a6e07b3f12cffc` 为 merge base，分别审查上游增量、本地增量、4 个文本冲突和 18 个双方修改交集。`docs/features/**` 描述本地能力，源码和测试决定最终行为；上游存在完整等价实现时采用上游实现，否则做语义并集。用户已明确要求只解决冲突，不修复随上游带入的缺陷；发现的上游问题只记录和披露。

**Tech Stack:** Git、Go 1.26.5、Gin、Ent、Wire、Redis、Vue 3、TypeScript、pnpm、Vitest。

---

### Task 1: 固定目标、创建分支并验证合并前基线

**Files:**
- Inspect: Git refs
- Inspect: `backend/cmd/server/VERSION`

- [x] **Step 1: 刷新上游并固定 0.1.160 main 边界**

```powershell
git fetch upstream main --tags --prune
git rev-parse upstream/main
git show upstream/main:backend/cmd/server/VERSION
git rev-parse "v0.1.160^{}"
git show "v0.1.160^{}:backend/cmd/server/VERSION"
```

Expected: `upstream/main=57914967cbb127ff715719c3879d881c10d75274` 且 `VERSION=0.1.160`。标签目标为 `8bfbc5ca99bf2c0ac96e0f29ffd35eb6aca27e62`，其文件仍是 `0.1.159`，因此不能只合标签。

- [x] **Step 2: 从本地 main 创建工作分支**

```powershell
git switch -c "feature/hy/10160_合并1.160版本" main
```

Expected: 新分支 `HEAD=main=5d5f157854b9a88cc57da1600095bb404b78ed45`。

- [x] **Step 3: 执行合并前 smoke 基线**

```powershell
go test -tags=unit -p 1 -count=1 ./internal/config ./internal/server/routes ./internal/handler
cmd.exe /c pnpm --dir frontend run typecheck
```

Expected: 三个后端包与前端类型检查退出 0；Windows Go 测试使用仓库内 cache 和 fresh `GOTMPDIR`。

### Task 2: 建立无提交 merge 并解决 4 个文本冲突

**Files:**
- Modify: `backend/cmd/server/wire.go`
- Regenerate: `backend/cmd/server/wire_gen.go`
- Modify: `frontend/src/components/layout/AppSidebar.vue`
- Modify: `frontend/src/router/index.ts`

- [x] **Step 1: 合入固定上游提交但不创建 commit**

```powershell
git merge --no-commit --no-ff 57914967cbb127ff715719c3879d881c10d75274
git rev-parse MERGE_HEAD
git ls-files -u
```

Expected: `MERGE_HEAD` 精确等于固定目标，Git 停在 4 个预演冲突上。

- [x] **Step 2: 合并 Wire 源图与生成产物冲突**

`backend/cmd/server/wire.go` 必须同时保留本地 `promptmetrics.ProviderSet`、Token Analysis/用户并发 preset/Prompt Metrics/quota flusher 清理链，以及上游 `securityaudit.ProviderSet`、`Application.PromptAudit` 和 `PromptAuditService.Shutdown`。`wire_gen.go` 只做 ours/theirs 语义并集，不能借解决生成冲突顺手修正上游 ProviderSet。

```powershell
git diff --cc -- backend/cmd/server/wire_gen.go
git add backend/cmd/server/wire.go backend/cmd/server/wire_gen.go
```

Expected: `wire_gen.go` 同时构造本地扩展和上游 Prompt Audit，`Application` 同时注入两者。已确认上游 `securityaudit.ProviderSet` 缺少 `PromptAdminService -> *PromptService` 绑定，导致当前源图执行 `go generate ./cmd/server` 失败；按用户要求不修复，只作为上游问题披露。

- [x] **Step 3: 合并管理端导航与路由**

`AppSidebar.vue` 保留本地图片生成、请求拦截、Prompt Metrics、Token Analysis 和组织用量，并采用上游 Security Audit 分组，把现有 Risk Control 与新 Prompt Audit 放入同组。`router/index.ts` 保留本地 `main` 已有路由和 backend-mode 边界，同时加入 `/admin/prompt-audit`。子管理员功能分支不在本次第一父历史中, 不属于本轮保留范围。

```powershell
git add backend/cmd/server/wire.go backend/cmd/server/wire_gen.go frontend/src/components/layout/AppSidebar.vue frontend/src/router/index.ts
git ls-files -u
rg -n "^(<<<<<<< |>>>>>>> |=======$)" backend frontend
```

Expected: unmerged index 和精确冲突标记均为空。

### Task 3: 审查 18 个双方修改交集与本地功能保留

**Files:**
- Inspect/Modify: `backend/cmd/server/main.go`
- Inspect/Modify: `backend/cmd/server/wire_gen_test.go`
- Inspect/Modify: `backend/internal/handler/content_moderation_helper.go`
- Inspect/Modify: `backend/internal/handler/gemini_v1beta_handler.go`
- Inspect/Modify: `backend/internal/handler/handler.go`
- Inspect/Modify: `backend/internal/handler/openai_gateway_handler.go`
- Inspect/Modify: `backend/internal/handler/wire.go`
- Inspect/Modify: `backend/internal/server/routes/admin.go`
- Inspect/Modify: `frontend/package.json`
- Inspect/Modify: `frontend/pnpm-lock.yaml`
- Inspect/Modify: `frontend/src/i18n/locales/en/admin/index.ts`
- Inspect/Modify: `frontend/src/i18n/locales/en/common.ts`
- Inspect/Modify: `frontend/src/i18n/locales/zh/admin/index.ts`
- Inspect/Modify: `frontend/src/i18n/locales/zh/common.ts`
- Inspect: `docs/features/**`

- [x] **Step 1: 对自动合并交集逐文件做三方审查**

```powershell
$paths = @(
  'backend/cmd/server/main.go',
  'backend/cmd/server/wire_gen_test.go',
  'backend/internal/handler/content_moderation_helper.go',
  'backend/internal/handler/gemini_v1beta_handler.go',
  'backend/internal/handler/handler.go',
  'backend/internal/handler/openai_gateway_handler.go',
  'backend/internal/handler/wire.go',
  'backend/internal/server/routes/admin.go',
  'frontend/package.json',
  'frontend/pnpm-lock.yaml',
  'frontend/src/i18n/locales/en/admin/index.ts',
  'frontend/src/i18n/locales/en/common.ts',
  'frontend/src/i18n/locales/zh/admin/index.ts',
  'frontend/src/i18n/locales/zh/common.ts'
)
foreach ($path in $paths) {
  git diff c2c19a7cbe8486ebb5b56834d1a6e07b3f12cffc..main -- $path
  git diff c2c19a7cbe8486ebb5b56834d1a6e07b3f12cffc..57914967cbb127ff715719c3879d881c10d75274 -- $path
  git diff HEAD -- $path
}
```

Expected: 每个交集文件确认本地语义与上游 Prompt Audit、Grok media、S3 step-up、依赖/i18n 变化是否同时存在；自动合并无语法正确但调用链缺失的问题。

- [x] **Step 2: 建立本地功能处置表**

至少覆盖 RequestArchive/RequestIntercept、Prompt Metrics、Prompt Risk/LLM judge、Token Analysis、组织用量、图片生成、用户并发 preset、OpenAI-compatible options/cache usage、默认 reasoning effort 和 quota flusher。每项标记为“保留、组合、由上游完整替代”，并列出源码与测试证据。

```powershell
git diff HEAD --diff-filter=D --name-only -- docs/features
git diff --name-status HEAD -- docs/features
```

Expected: 不删除 tracked `docs/features/**`；只有上游完整覆盖且测试合同一致时才移除本地重复实现。

### Task 4: 更新稳定知识和追加合并记录

**Files:**
- Modify: `llm-wiki/wiki/README.md`
- Modify: `llm-wiki/wiki/backend.md`
- Modify: `llm-wiki/wiki/frontend.md`
- Modify: `llm-wiki/wiki/ops.md`
- Modify: `llm-wiki/wiki/data-and-domain.md`
- Modify: `llm-wiki/wiki/security-and-reliability.md`
- Modify: `docs/features/sub2api -merage-list.md`
- Create: `docs/reviews/2026-07-17-sub2api-v0.1.160-merge-review.md`

- [x] **Step 1: 按源码更新 durable wiki**

记录上游 Prompt Audit 的启动/关闭、路由、存储、扫描、完整 prompt 审计隐私边界，Grok media eligibility 修复，S3 step-up，以及实际 migration/config/依赖变化。只写稳定事实、相对路径与验证方式，保持 LF，不写密钥或测试流水账。

- [x] **Step 2: 仅追加 merge ledger 条目**

追加日期、分支、`upstream/main@57914967c`、merge base、133 个上游文件、冲突文件、处置方式、本地功能映射和真实验证结果。`Merge commit` 写“待用户审核，尚未创建”，不得伪造 SHA。

### Task 5: 定向回归、完整验证与 commit 前硬门

**Files:**
- Test: 上述冲突与交集模块的现有测试
- Verify: 全仓库测试、构建和 Git 状态

- [x] **Step 1: 先跑冲突与上游新增能力定向回归**

```powershell
Set-Location backend
go test -tags=unit -p 1 -count=1 ./cmd/server ./internal/securityaudit ./internal/server/routes ./internal/handler ./internal/service
Set-Location ..
cmd.exe /c pnpm --dir frontend exec vitest run src/features/prompt-audit src/components/layout src/router
```

Expected: Wire、Prompt Audit、路由/handler/service 及前端导航与路由相关回归退出 0。

- [x] **Step 2: 执行完整后端验证**

```powershell
Set-Location backend
go test -tags=unit -p 1 -count=1 ./...
go test -tags=integration -p 1 -count=1 ./...
golangci-lint run ./...
golangci-lint run --new-from-rev main ./...
go mod tidy -diff
Set-Location ..
```

Expected: 使用 repo-local Go cache 和每轮 fresh `GOTMPDIR`。测试真实失败为 0；全量 lint、增量 lint 和 module diff 分开记录，既有债务不得写成合并新增问题。

- [x] **Step 3: 执行完整前端与 embed 验证**

```powershell
cmd.exe /c pnpm --dir frontend run lint:check
cmd.exe /c pnpm --dir frontend run typecheck
cmd.exe /c pnpm --dir frontend exec vitest run
cmd.exe /c pnpm --dir frontend run build
Set-Location backend
go build -tags embed -trimpath -o .gocache/sub2api-embed-v0160.exe ./cmd/server
Set-Location ..
```

Expected: lint、typecheck、Vitest、生产构建与最新前端产物的 embed build 均记录实际退出码和测试计数。

- [x] **Step 4: 执行 Git 与范围硬门**

```powershell
git rev-parse HEAD main MERGE_HEAD
git ls-files -u
git diff --check
git diff --cached --check
git diff HEAD --check
git diff HEAD --diff-filter=D --name-only -- docs/features
rg -n "^(<<<<<<< |>>>>>>> |=======$)" -g "!*lock*"
git status --short --branch
```

Expected: `HEAD=main=5d5f157854b9a88cc57da1600095bb404b78ed45`，`MERGE_HEAD=57914967cbb127ff715719c3879d881c10d75274`；无 unmerged、冲突标记、whitespace error、`docs/features` 删除或未披露的工作树变化。

- [x] **Step 5: 通知用户审核并停止**

汇报冲突决策、18 个交集审查、本地功能处置表、文件统计、测试结果和残余风险。保持 `git merge --no-commit --no-ff` 状态，不运行 `git commit`、push、PR 或部署，等待用户明确授权。
