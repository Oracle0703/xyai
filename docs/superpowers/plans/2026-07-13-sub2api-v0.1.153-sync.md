# sub2api v0.1.153 固定上游同步实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox syntax for tracking.

**Goal:** 基于 0.1.151 合并 Wei-Shaw/sub2api `55ed0ab0da367183d97c15659e33ae9e83f6ff90`，保留两侧功能并完成文档和全量验证。

**Architecture:** 使用一个双父 merge commit 保留本地与上游拓扑；第一父固定为 151 基线 `5e6e85568792`，第二父固定为上游 0.1.153 `55ed0ab0da36`。冲突按 merge-base/ours/theirs 三方审查，后续文档与依赖清理使用独立收口提交，不改写 merge commit。

**Tech Stack:** Git、Go 1.26.5、Gin、Ent、Redis、Vue 3、TypeScript、pnpm、Vitest。

---

### Task 1: 固定合并边界

**Files:**
- Inspect: Git refs and commit graph

- [x] **Step 1: 确认 151 基线**

Run: `git show -s --format=%H 5e6e85568792096abfc9d0114b9172dad6256cb7`

Expected: 输出固定的 151 基线提交。

- [x] **Step 2: 合入固定 0.1.153 SHA**

Run: `git merge --no-ff 55ed0ab0da367183d97c15659e33ae9e83f6ff90`

Expected: 生成双父 merge commit，或进入可审查冲突状态。

- [x] **Step 3: 排除后续上游提交**

Run: `git merge-base --is-ancestor 7d239d62e HEAD`

Expected: 退出码 1，表示后续上游 head 未进入分支。

### Task 2: 解决冲突并验证组合语义

**Files:**
- Modify: `README_CN.md`
- Modify: `backend/go.mod`
- Modify: `backend/internal/pkg/apicompat/chatcompletions_responses_bridge.go`
- Modify: `backend/internal/server/routes/gateway.go`
- Modify: `backend/internal/service/concurrency_service.go`
- Modify: `frontend/src/components/account/CreateAccountModal.vue`
- Test: 对应 Go/Vitest 测试文件

- [x] **Step 1: 三方审查全部冲突**

Run: `git ls-files -u`

Expected: 解决前列出 6 个冲突；解决后无输出。

- [x] **Step 2: 保留本地 options adapter 与上游 additional tools**

Run: `go test -tags=unit -p 1 -count=1 ./internal/pkg/apicompat`

Expected: 所有 bridge 和工具回程测试通过。

- [x] **Step 3: 保留本地中间件、并发错误与账号 preset**

Run: `go test -tags=unit -p 1 -count=1 ./internal/server/routes ./internal/service ./internal/handler`

Run: `cmd.exe /c pnpm --dir frontend exec vitest run src/components/account`

Expected: 路由、并发、账号凭据测试全部通过。

- [x] **Step 4: 创建 merge commit**

Run: `git commit`

Expected: `0d65f65a20df` 的 parents 为 `5e6e85568 55ed0ab0d`。

### Task 3: 文档、依赖与最终验证

**Files:**
- Modify: `backend/go.sum`
- Modify: `docs/features/sub2api -merage-list.md`
- Create: `docs/reviews/2026-07-13-upstream-55ed0ab-merge-review.md`
- Modify: `llm-wiki/wiki/README.md`
- Modify: `llm-wiki/wiki/backend.md`
- Modify: `llm-wiki/wiki/frontend.md`
- Modify: `llm-wiki/wiki/ops.md`
- Modify: `llm-wiki/wiki/data-and-domain.md`
- Modify: `llm-wiki/wiki/security-and-reliability.md`
- Create: `frontend/src/components/account/README.md`
- Create: `frontend/src/components/keys/README.md`
- Modify: `frontend/src/components/common/README.md`

- [x] **Step 1: 规范化 Go 依赖**

Run: `cd backend; go mod tidy; go mod tidy -diff`

Expected: 第二条命令退出 0 且无 diff。

- [x] **Step 2: 写入追加型合并记录和稳定知识**

Run: `git diff -- docs/features 'llm-wiki/wiki' frontend/src/components`

Expected: ledger 只在文件尾新增；wiki/README 内容与 0.1.153 源码一致。

- [x] **Step 3: 执行最终全量验证**

Run: `go test -tags=unit -p 1 -count=1 ./...`

Run: `go test -tags=integration -p 1 -count=1 ./...`

Run: `cmd.exe /c pnpm --dir frontend run lint:check`

Run: `cmd.exe /c pnpm --dir frontend run typecheck`

Run: `cmd.exe /c pnpm --dir frontend exec vitest run`

Run: `cmd.exe /c pnpm --dir frontend run build`

Expected: 所有测试与构建退出 0；全仓 lint 历史债务单列，差异 lint 必须为 0 issues。

- [x] **Step 4: 提交收口并审计**

Run: `git add <本轮文件>; git commit`

Run: `git status --short --branch; git rev-list --parents -n 3 HEAD`

Expected: 文档收口提交位于既有 `e09179991` 之后，工作树干净，merge 第二父仍为固定目标 SHA。
