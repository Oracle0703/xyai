# sub2api v0.1.161 Upstream Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 基于本地 `main@332fdbd0b84619cfb1da6fcb57b65d4d9263b2e9`，把 `Wei-Shaw/sub2api main@d4b9797ff72024960a035cf22fdd8f213e149169` 的 `0.1.161` 固定边界合入 `feature/hy/10161_合并1.161版本`，保留未被上游完整替代的本地功能，并在用户审核前保持未提交 merge 状态。

**Architecture:** 使用 `git merge --no-commit --no-ff` 保留双父拓扑，以 `57914967cbb127ff715719c3879d881c10d75274` 为 merge base，逐一审查 4 个文本冲突和 43 个双方修改交集。`docs/features/**`、本地源码和测试共同定义本地能力；上游存在完整等价实现时采用上游实现，否则做最小语义并集。用户明确要求不修复上游缺陷，因此发现的 upstream-native 问题只记录、验证和披露，不改生产实现。

**Tech Stack:** Git、Go 1.26.5、Gin、Ent、Wire、Redis、Vue 3、TypeScript、pnpm、Vitest。

---

### Task 1: 固定目标、创建分支并验证合并前基线

**Files:**
- Inspect: Git refs
- Inspect: `backend/cmd/server/VERSION`

- [x] **Step 1: 刷新上游并固定 0.1.161 main 边界**

```powershell
git fetch upstream main --tags --prune
git rev-parse upstream/main
git show upstream/main:backend/cmd/server/VERSION
git show v0.1.161:backend/cmd/server/VERSION
```

Expected: `upstream/main=d4b9797ff72024960a035cf22fdd8f213e149169` 且 `VERSION=0.1.161`。`v0.1.161@19149ca196eeae4a4482e5299dc6fa4ba0b06c8c` 内仍为 `0.1.160`，因此固定采用其后的 version-sync commit。

- [x] **Step 2: 从本地 main 创建工作分支**

```powershell
git switch -c "feature/hy/10161_合并1.161版本" main
```

Expected: 新分支 `HEAD=main=332fdbd0b84619cfb1da6fcb57b65d4d9263b2e9`，除本计划外没有已有工作区改动。

- [x] **Step 3: 执行合并前 smoke 基线**

```powershell
Set-Location backend
$env:GOCACHE = (Resolve-Path '.gocache/review-cache').Path
$env:GOMODCACHE = (Resolve-Path '.gocache/review-gopath/pkg/mod').Path
$env:GOTMPDIR = Join-Path (Resolve-Path '.gocache').Path ("run-tmp-v0161-baseline-" + (Get-Date -Format 'yyyyMMddHHmmss'))
New-Item -ItemType Directory -Force -Path $env:GOTMPDIR | Out-Null
go test -tags=unit -p 1 -count=1 ./internal/config ./internal/server/routes ./internal/handler
Set-Location ..
cmd.exe /c pnpm --dir frontend run typecheck
```

Expected: 后端高风险 smoke 包与前端类型检查退出 0。若基线失败，先确认是否为 Windows `.test.exe` 启动锁；真实代码失败必须在合并前披露。

### Task 2: 建立无提交 merge 并解决 4 个文本冲突

**Files:**
- Regenerate: `backend/cmd/server/wire_gen.go`
- Modify: `backend/internal/server/routes/gateway.go`
- Modify: `frontend/src/App.vue`
- Modify: `frontend/src/components/account/CreateAccountModal.vue`

- [x] **Step 1: 合入固定上游提交但不创建 commit**

```powershell
git merge --no-commit --no-ff d4b9797ff72024960a035cf22fdd8f213e149169
git rev-parse MERGE_HEAD
git ls-files -u
```

Expected: `MERGE_HEAD` 精确等于固定目标，Git 停在预演得到的 4 个文本冲突上。

- [x] **Step 2: 组合 Wire 与网关路由冲突**

`wire_gen.go` 以合并后的 `wire.go`、ProviderSet 和构造器重新生成，必须同时保留本地 Prompt Metrics、Token Analysis、用户并发 preset、quota flusher、RequestIntercept 依赖，以及上游 Ops ingress reject、auth-cache invalidation worker、Prompt Audit 等启动/清理链。`gateway.go` 保留本地 RequestArchive/RequestIntercept middleware 顺序，同时合入上游 Grok 视频 content 代理、入口拒绝采集和新鉴权边界。

```powershell
Set-Location backend
go generate ./cmd/server
Set-Location ..
git add backend/cmd/server/wire_gen.go backend/internal/server/routes/gateway.go
```

Expected: 生成命令若因上游自身 ProviderSet 缺陷失败，只记录失败并按双方现有生成图手工组合；不得新增本地修复。网关归档/拦截与上游 content route 均有测试锚点。

- [x] **Step 3: 组合前端入口与账号创建冲突**

`App.vue` 采用上游 `utils/branding.ts` 的 `updateFavicon`，保留本地 `admin-permission-denied` 监听、子管理员权限刷新/退出/重定向和 `canAccessAdmin` 合规判断。`CreateAccountModal.vue` 保留本地 OpenAI-compatible provider preset、placeholder/credentials 适配，同时加入上游 `upstream_billing_probe_enabled` toggle、创建后 probe 和失败 warning。

```powershell
git add frontend/src/App.vue frontend/src/components/account/CreateAccountModal.vue
git ls-files -u
rg -n "^(<<<<<<< |>>>>>>> |=======$)" backend frontend
```

Expected: unmerged index 和精确冲突标记均为空，双方独立能力都可由现有测试定位。

### Task 3: 审查 43 个自动合并交集与本地功能保留

**Files:**
- Inspect/Modify: 43 个 `main` 与 `upstream/main` 双方修改文件
- Inspect: `docs/features/**`
- Inspect: 本地功能对应源码与测试

- [x] **Step 1: 对自动合并交集逐文件做三方审查**

```powershell
$base = '57914967cbb127ff715719c3879d881c10d75274'
$local = @(git diff --name-only "$base..main")
$upstream = @(git diff --name-only "$base..d4b9797ff72024960a035cf22fdd8f213e149169")
$both = @($local | Where-Object { $upstream -contains $_ } | Sort-Object -Unique)
foreach ($path in $both) {
  git diff "$base..main" -- $path
  git diff "$base..d4b9797ff72024960a035cf22fdd8f213e149169" -- $path
  git diff HEAD -- $path
}
```

Expected: 43 个交集文件逐一确认调用签名、中间件顺序、ProviderSet、权限合同、配置默认值、前端 payload/i18n 与上游新增逻辑均未被静默覆盖。只修改语义冲突，不清理或重构上游代码。

- [x] **Step 2: 建立本地功能处置表**

至少核对 RequestArchive/RequestIntercept、Prompt Metrics、Prompt Risk/LLM judge、Token Analysis、组织用量、图片生成/支付、用户并发 preset/`ConcurrencyCacheError`、OpenAI-compatible preset/options/cache usage、默认 reasoning effort、quota flusher 和子管理员权限。每项标记为“保留、组合、上游完整替代”，并列出源码与测试证据。

```powershell
git diff HEAD --diff-filter=D --name-only -- docs/features
git diff --name-status HEAD -- docs/features
```

Expected: 不删除 tracked `docs/features/**`；只有上游实现和测试合同完整等价时才移除本地重复实现。

### Task 4: 更新稳定知识与追加合并记录

**Files:**
- Modify: `llm-wiki/wiki/README.md`
- Modify: `llm-wiki/wiki/backend.md`
- Modify: `llm-wiki/wiki/frontend.md`
- Modify: `llm-wiki/wiki/ops.md`
- Modify: `llm-wiki/wiki/data-and-domain.md`
- Modify: `llm-wiki/wiki/security-and-reliability.md`
- Modify: `frontend/src/components/account/README.md`
- Modify: `docs/features/sub2api -merage-list.md`
- Create: `docs/reviews/2026-07-19-sub2api-v0.1.161-merge-review.md`

- [x] **Step 1: 按最终源码更新 durable wiki 与组件说明**

记录 0.1.161 的安全开关默认值、Grok 受保护视频代理、模型级临时隔离、auth-cache invalidation outbox、入口拒绝聚合、OpenAI WS/Responses 修复、相关 migration/config/deploy 变化，以及本地能力组合结果。只写稳定事实、相对路径与验证方式，保持 LF，不写密钥或测试流水账。

- [x] **Step 2: 仅在 merge ledger 文件末尾追加条目**

条目包含日期、分支、上游分支/固定 SHA、merge base、版本、提交/文件统计、4 个冲突文件、43 个交集审查、处理方式、本地功能映射、上游原生问题与真实验证结果。`Merge commit` 写“待用户审核，尚未创建”，不得伪造 SHA。

### Task 5: 定向回归、完整验证与 commit 前硬门

**Files:**
- Test: 冲突与交集模块现有测试
- Verify: 全仓库测试、生成、构建和 Git 状态

- [x] **Step 1: 先跑冲突与高风险交集定向回归**

```powershell
Set-Location backend
go test -tags=unit -p 1 -count=1 ./cmd/server ./internal/server/routes ./internal/server/middleware ./internal/handler ./internal/service
Set-Location ..
cmd.exe /c pnpm --dir frontend exec vitest run src/components/account/__tests__/CreateAccountModal.spec.ts src/utils/__tests__/branding.spec.ts
```

Expected: Wire、gateway/middleware、handler/service 与两个前端冲突面的现有回归完成；失败必须区分冲突引入、上游原生和环境噪音。

- [x] **Step 2: 执行完整后端验证**

```powershell
Set-Location backend
go generate ./cmd/server
go test -tags=unit -p 1 -count=1 ./...
go test -tags=integration -p 1 -count=1 ./...
golangci-lint run ./...
golangci-lint run --new-from-rev main ./...
go mod tidy -diff
Set-Location ..
```

Expected: 每轮使用 repo-local cache 和新的 `GOTMPDIR`。全量与增量 lint、module diff 和任何 Windows 安全软件拦截分别记录；不得为让测试变绿而修复上游 bug。

Actual: 完整 unit 只失败于本地 `main` 既有的 `/auth/me` `admin_permissions: null` contract mismatch；完整 integration 退出 0。全量 lint 为 28 条既有债务，增量 lint 为 `0 issues`；`go mod tidy -diff` 只建议删除 `main` 已存在的 6 组 Wire CLI 传递校验和。Wire 连续生成稳定，SHA-256 为 `5F2930F8...3237AC`。按用户边界均只记录，没有修改生产逻辑。

- [x] **Step 3: 执行完整前端与 embed 验证**

```powershell
cmd.exe /c pnpm --dir frontend run lint:check
cmd.exe /c pnpm --dir frontend run typecheck
cmd.exe /c pnpm --dir frontend exec vitest run
cmd.exe /c pnpm --dir frontend run build
Set-Location backend
go build -tags embed -trimpath -o .gocache/sub2api-embed-v0161.exe ./cmd/server
Set-Location ..
```

Expected: lint、typecheck、Vitest、生产构建与最新前端产物的 embed build 均记录实际退出码、测试计数和产物信息。

Actual: frontend lint、typecheck、Vitest 200 files / 1373 tests 通过；build 981 modules / 17.81s，退出 0。embed build 退出 0，产物 149,794,304 bytes。

- [x] **Step 4: 执行 Git、范围与上游边界硬门**

```powershell
git fetch upstream main --tags --prune
git rev-parse HEAD main MERGE_HEAD
git ls-files -u
git diff --check
git diff --cached --check
git diff HEAD --check
git diff HEAD --diff-filter=D --name-only -- docs/features
rg -n "^(<<<<<<< |>>>>>>> |=======$)" -g "!*lock*"
git status --short --branch
```

Expected: `HEAD=main=332fdbd0b84619cfb1da6fcb57b65d4d9263b2e9`，`MERGE_HEAD=d4b9797ff72024960a035cf22fdd8f213e149169`；无 unmerged、未说明的 unstaged/untracked、冲突标记、whitespace error 或 `docs/features` 删除。即使 upstream 在长会话中前进，也不越过已固定的 0.1.161 边界。

Actual: 收尾 fetch 后 `upstream/main=MERGE_HEAD=d4b9797ff72024960a035cf22fdd8f213e149169`；267 个 staged 文件，`+14033/-1540`。unmerged、unstaged、untracked、精确冲突标记和 `docs/features` 删除均为 0，worktree/cached/HEAD 三种 `diff --check` 均退出 0。

- [x] **Step 5: 通知用户审核并停止**

汇报 4 个冲突决策、43 个交集审查、本地功能处置表、文件统计、测试结果、上游原生问题和环境限制。保持 `git merge --no-commit --no-ff` 状态，不运行 `git commit`、push、PR 或部署，等待用户明确授权。

Actual: 用户审核后明确接受本地 `main` 既有 `/auth/me` `admin_permissions: null` contract mismatch 暂不修复，并授权创建 merge commit `e3e6b52da43a5be351cf59089976759eebc28376`；未 push、未创建 PR、未部署。
