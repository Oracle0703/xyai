# sub2api v0.1.161 固定边界合并复核

## 复核结论

| 项目 | 结论 |
|---|---|
| 工作分支 | `feature/hy/10161_合并1.161版本` |
| 本地第一父 | `332fdbd0b84619cfb1da6fcb57b65d4d9263b2e9`（本地 `main`） |
| 上游第二父 | `d4b9797ff72024960a035cf22fdd8f213e149169`（固定 `upstream/main`） |
| 上游版本 | `0.1.161`; tag `v0.1.161` 指向 `19149ca196eeae4a4482e5299dc6fa4ba0b06c8c`, 但该提交的 `VERSION` 仍为 `0.1.160` |
| 合并状态 | `git merge --no-commit --no-ff`; `MERGE_HEAD=d4b9797ff72024960a035cf22fdd8f213e149169`; 尚未 commit/push |
| 文本冲突 | 4 个，均按双方语义解决 |
| 双方修改交集 | 43 个文件，均已按 merge base、本地 main 和固定 upstream 三方审查 |
| 本地功能 | 11 类重点能力均保留或与上游组合；`docs/features/` tracked 删除数为 0 |
| 用户边界 | 只解决冲突和保留本地功能所需兼容；不修复随上游带入的 bug |

本报告只确认本轮固定边界、冲突决策、功能保留和实际验证结果。上游或本地基线自身缺陷只记录和归因，不在本分支顺手修复。

## 1. 合并边界与拓扑

| 事实 | 值 |
|---|---|
| Remote | `upstream=https://github.com/Wei-Shaw/sub2api.git` |
| Upstream branch | `main` |
| First parent / local `main` | `332fdbd0b84619cfb1da6fcb57b65d4d9263b2e9` |
| Merge base | `57914967cbb127ff715719c3879d881c10d75274` |
| Second parent / `MERGE_HEAD` | `d4b9797ff72024960a035cf22fdd8f213e149169` |
| Merge commit | **待用户审核，尚未创建** |
| `v0.1.161` tag commit | `19149ca196eeae4a4482e5299dc6fa4ba0b06c8c` |
| Upstream delta | 62 commits；257 files；`+13643 / -1523` |

版本边界必须使用 `d4b9797ff`：tag commit 已包含功能合并，但后续提交才把 `backend/cmd/server/VERSION` 同步为 `0.1.161`。本轮固定第二父，不追随审核期间可能继续前进的 upstream head。

## 2. 文本冲突复核

| 文件 | ours 意图 | upstream 意图 | 最终处理 |
|---|---|---|---|
| `backend/cmd/server/wire_gen.go` | Prompt Metrics、Token Analysis、用户并发 preset、quota flusher、RequestIntercept 依赖及完整 cleanup。 | Ops ingress rejection aggregator、auth-cache invalidation worker、Prompt Audit 等新增构造和生命周期。 | 以合并后的 provider 源图重新生成，同时保留双方构造、启动和逆序清理链。 |
| `backend/internal/server/routes/gateway.go` | 所有本地 gateway 路由继续挂载 RequestArchive / RequestIntercept。 | 纯文本 body limit、Grok video content 同源代理和入口拒绝相关边界。 | 保持本地 middleware 顺序，并把上游 text body limit 与受保护 content 路由接入对应路由组。 |
| `frontend/src/App.vue` | 子管理员权限拒绝事件、权限刷新、退出和路由恢复。 | 统一复用 `utils/branding.ts` 更新标题和 favicon。 | 采用上游 branding helper，同时保留完整的子管理员权限恢复流程。 |
| `frontend/src/components/account/CreateAccountModal.vue` | OpenAI-compatible provider preset、base URL、placeholder 和 endpoint capability。 | OpenAI API Key 自动 upstream billing probe toggle、payload 和创建后首次 probe。 | 两套独立状态和 submit/reset 链同时保留；自动 probe 不覆盖 compatible preset。 |

冲突解决后 `git ls-files -u` 为空，上述文件没有精确冲突标记。

## 3. 自动合并交集复核

43 个双方修改文件已逐项对照 `57914967c..main`、`57914967c..d4b9797ff` 与最终 index，重点检查以下合同：

| 检查面 | 结果 |
|---|---|
| Wire / lifecycle | 本地后台服务和上游 ingress/auth-cache 服务均在 provider、Application 与 cleanup 链中。 |
| Gateway / middleware | RequestArchive/RequestIntercept 与上游 text body limit、Grok content 路由并存。 |
| API / DTO / repository | 子管理员权限字段、上游 API Key cache outbox 与 repository 接口变化未互相覆盖。 |
| Settings / config | 本地 reasoning effort、archive/intercept 与上游 ingress、step-up、cache 设置合同并存。 |
| Frontend payload / i18n | compatible preset、billing auto probe、branding、settings 类型和中英文 key 同步。 |

没有发现 4 个文本冲突之外还需要修改的语义冲突；没有借自动合并交集清理或重构上游代码。

## 4. 本地功能保留矩阵

| 本地能力 | 决策 | 代码/测试证据 | 复核说明 |
|---|---|---|---|
| RequestArchive / RequestIntercept | **组合** | `backend/internal/server/middleware/request_archive.go`; `request_intercept.go`; route/middleware tests | 冲突路由保留 middleware，并接入上游新增入口。 |
| Prompt Metrics | **保留** | `backend/internal/service/promptmetrics/`; `frontend/src/views/admin/PromptMetricsView.vue` | middleware、管理端入口和生命周期未被上游 Ops 能力替代。 |
| Prompt Risk / LLM judge | **保留** | `backend/internal/service/prompt_risk*.go`; moderation tests | 规则、judge、fail-open 和防递归合同继续存在。 |
| Token Analysis | **保留** | `token_analysis_*`; `frontend/src/views/admin/TokenAnalysisView.vue` | handler/service/repository/UI 与后台索引入口完整。 |
| 组织用量 | **保留** | `organization_usage_*`; `OrganizationUsageView.vue` | API、聚合、导出和页面未被上游覆盖。 |
| 图片生成与支付 | **组合** | `ImageGenView.vue`; image intent; payment routes/services | 本地域继续存在，并采用上游 Grok 受保护视频增量。 |
| 用户并发 preset / `ConcurrencyCacheError` | **保留** | `user_concurrency_preset_*`; `concurrency_service.go`; tests | runner、缓存错误和 503 合同继续存在。 |
| OpenAI-compatible preset/options/cache usage | **组合** | `openaiCompatibleProviderPresets.ts`; `internal/pkg/apicompat/`; gateway usage tests | 本地 preset 与上游自动 billing probe、兼容桥接增量独立生效。 |
| 默认 reasoning effort | **保留** | `backend/internal/config/config.go`; `openai_gateway_chat_completions.go`; tests | 配置默认值和注入路径未被本轮上游覆盖。 |
| user-platform quota flusher | **保留** | `user_platform_quota_flusher.go`; Wire cleanup; repository tests | dirty snapshot、批量落库和 shutdown 仍接入。 |
| 子管理员权限 | **保留** | `admin_permission.go`; admin middleware/routes; `frontend/src/utils/adminPermissions.ts` | DB-authoritative 权限、路由白名单、侧栏过滤和拒绝恢复均存在。 |

没有发现可由 upstream 完整等价替代并安全移除的上述本地能力，因此“上游完整替代”项为 0。

## 5. 上游 0.1.161 事实

| 领域 | 合入行为 |
|---|---|
| HTTP ingress | 增加 header 读取超时/大小基线、纯文本 body 上限、拒绝原因有界聚合和 Ops 健康查询。 |
| API Key 鉴权缓存 | migration 184 增加只保存 key SHA-256 的 durable invalidation outbox；worker、Redis subscriber 和二次失效让多实例 L1/L2 收敛。 |
| 安全开关 | session binding 与 step-up 默认关闭；开关保存、关闭 step-up 的二次验证和旧客户端省略字段保持现值。 |
| Grok media | 生成请求绑定原 user/API key 与实际账号；status/content 强制所有权并由网关代理签名内容。 |
| 调度 | 已知模型时临时不可调度规则只隔离 `(account, model)`；401 或未知模型保留账号级语义。 |
| OpenAI WS / Responses | 合入 passthrough lifecycle、读错误和 Anthropic/Responses 流式兼容修复。 |
| Frontend / deploy | 首屏与运行时 branding 统一；Docker cross-build、Caddy/Redis edge 配置更新。 |

## 6. 已披露且未修复的问题

| 归因 | 项目 | 证据 | 本轮处置 |
|---|---|---|---|
| 本地 `main` 既有 | `GET /api/v1/auth/me` API contract 期望不含 `admin_permissions`，但普通用户 DTO 实际序列化为 `"admin_permissions": null`。 | `main` 的 `dto.User` 字段无 `omitempty`，`UserFromService` 已映射该字段；本轮 `git diff --cached HEAD -G "admin_permissions|AdminPermissions"` 没有生产映射改动。 | 不修改。该问题早于 0.1.161 merge，与 4 个冲突无关，等待单独任务处理。 |
| 上游安全变更 | 删除 API Key 不再复制明文 key 到 deleted audit，并删除旧明文归因测试。 | 上游删除 `ops_repo_lookup_deleted_key_audit_integration_test.go`，migration 184/outbox 与 finalizer 采用 hash/有界维度。 | 采用上游实现；不是本地功能丢失，也不恢复旧明文审计。 |

未发现需要在本合并分支修复的 upstream-native bug；后续完整测试若暴露上游失败，只在本节补充归因和证据，不改生产实现。

## 7. 验证结果

| 验证 | 当前结果 |
|---|---|
| Backend targeted | 6 个高风险包通过。 |
| Backend unit | 完整运行到结尾，退出 1；仅 `internal/server` 的 `TestAPIContracts/GET_/api/v1/auth/me` 失败，实际比预期多出 `"admin_permissions": null`。该失败来自本地 `main` 既有 DTO/contract 不一致，本轮不修复。 |
| Backend integration | 完整运行到结尾，退出 0；所有 integration-tag 测试包通过。 |
| Backend lint | 全量报 28 条本地 `main` 既有债务（15 errcheck、1 gofmt、3 staticcheck、9 unused）；`golangci-lint run --new-from-rev main ./...` 退出 0，`0 issues`。 |
| Go modules | `go mod tidy -diff` 退出 1，只建议删除 6 组、12 行 Wire CLI 传递校验和；6 组均存在于本地 `main`，模块文件未修改。 |
| Wire generation | 连续两轮 `go generate ./cmd/server` 退出 0；生成前、第一次、第二次 SHA-256 均为 `5F2930F87591C97D900E18DEC4BDDE672EA280F1151FC062C45138B3DA3237AC`，相对 index 无漂移。 |
| Frontend targeted | 5 files / 27 tests passed。 |
| Frontend full | lint、typecheck、Vitest 200 files / 1373 tests passed；生产 build 退出 0，981 modules，17.81s。Browserslist 数据、mixed dynamic/static import 和大 chunk warning 不影响退出码。 |
| Embed build | 使用最新前端 dist 构建退出 0；产物 149,794,304 bytes，SHA-256 `B3B7E19D7A0FF2B96D8EA48580614234A86D753D95929470FE9CCE0387225B27`。 |
| Git hard gates | 最终 staged snapshot 为 267 files / `+14033 -1540`；`HEAD=main@332fdbd0b`、`MERGE_HEAD=upstream/main@d4b9797ff`。无 unmerged、unstaged、untracked、精确冲突标记、whitespace error 或 `docs/features` 删除；收尾 fetch 确认 `upstream/main` 未越过固定边界。 |

## 8. Commit gate

当前保持 `git merge --no-commit --no-ff`。完整验证、merge ledger 和最终 staged 硬门已收口，现通知用户审核；审核前不创建 merge commit，不 push、不创建 PR、不部署。
