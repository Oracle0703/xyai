# sub2api v0.1.160 固定边界合并复核

## 复核结论

| 项目 | 结论 |
|---|---|
| 工作分支 | `feature/hy/10160_合并1.160版本` |
| 本地第一父 | `5d5f157854b9a88cc57da1600095bb404b78ed45`（本地 `main`） |
| 上游第二父 | `57914967cbb127ff715719c3879d881c10d75274`（固定 `upstream/main`） |
| 上游版本 | `0.1.160`; tag `v0.1.160` 指向 `8bfbc5ca99bf2c0ac96e0f29ffd35eb6aca27e62`, 但该提交的 `VERSION` 仍为 `0.1.159` |
| 合并状态 | `git merge --no-commit --no-ff` 进行中；等待用户审核, 尚未创建 merge commit |
| 文本冲突 | 4 个, 均按双方语义解决 |
| 双方修改交集 | 18 个文件：4 个文本冲突 + 14 个自动合并交集, 均已审查 |
| 本地功能 | 10 类能力均保留或与上游组合；`docs/features/` tracked 删除数为 0 |
| 用户边界 | 只解决冲突和保留本地功能所需兼容；不修复随上游带入的 bug |

本报告只确认本轮合并边界、冲突决策、功能保留和实际验证结果。上游自身缺陷列入残余风险, 不在本分支顺手修复。

## 1. 合并边界与拓扑

| 事实 | 值 |
|---|---|
| Remote | `upstream=https://github.com/Wei-Shaw/sub2api.git` |
| Upstream branch | `main` |
| First parent / local `main` | `5d5f157854b9a88cc57da1600095bb404b78ed45` |
| Merge base | `c2c19a7cbe8486ebb5b56834d1a6e07b3f12cffc` |
| Second parent / `MERGE_HEAD` | `57914967cbb127ff715719c3879d881c10d75274` |
| `v0.1.160` tag target | `8bfbc5ca99bf2c0ac96e0f29ffd35eb6aca27e62` |
| Upstream delta | 20 commits; 133 files; `+19766 / -113` |

版本边界必须使用 `57914967c`：tag target 先合入功能, 后续提交才把 `backend/cmd/server/VERSION` 同步为 `0.1.160`。本轮没有追随审核期间可能继续前进的 upstream head。

## 2. 文本冲突复核

| 文件 | ours 意图 | upstream 意图 | 最终处理 |
|---|---|---|---|
| `backend/cmd/server/wire.go` | Prompt Metrics、Token Analysis、用户并发 preset、quota flusher 及对应 cleanup。 | `securityaudit.ProviderSet`、`Application.PromptAudit`、启动/关闭生命周期。 | ProviderSet、Application 字段和 cleanup 做语义并集。 |
| `backend/cmd/server/wire_gen.go` | 保留本地扩展的真实构造和清理链。 | 构造 Prompt Audit 并注入 handler/server。 | 组合双方现有生成结果；不借生成文件冲突修复 upstream ProviderSet。 |
| `frontend/src/components/layout/AppSidebar.vue` | 请求拦截、Prompt Metrics、Token Analysis、组织用量和本地权限过滤。 | 新增 Security Audit 分组与 Prompt Audit 入口。 | 采用上游 Security Audit 分组容纳 Risk Control / Prompt Audit, 保留独立 Request Intercept 及其他本地入口。 |
| `frontend/src/router/index.ts` | `/admin/request-intercept` 和全部本地 admin 路由/守卫。 | `/admin/prompt-audit` 与 risk-control gate。 | 两套路由并存, Prompt Audit 使用 `requiresRiskControl`。 |

冲突解决后 `git ls-files -u` 为空, 上述文件没有精确冲突标记。

## 3. 自动合并交集复核

除 4 个文本冲突外, 以下 14 个双方修改文件逐项对照 merge base、本地 main 和 upstream：

```text
backend/cmd/server/main.go
backend/cmd/server/wire_gen_test.go
backend/internal/handler/content_moderation_helper.go
backend/internal/handler/gemini_v1beta_handler.go
backend/internal/handler/handler.go
backend/internal/handler/openai_gateway_handler.go
backend/internal/handler/wire.go
backend/internal/server/routes/admin.go
frontend/package.json
frontend/pnpm-lock.yaml
frontend/src/i18n/locales/en/admin/index.ts
frontend/src/i18n/locales/en/common.ts
frontend/src/i18n/locales/zh/admin/index.ts
frontend/src/i18n/locales/zh/common.ts
```

唯一额外语义适配是把本地 `X-Sub2API-Prompt-Risk-Judge` 签名头从 handler 的 `securityaudit.Request` 继续传入 legacy moderation。否则 judge 经本网关 HTTP 回环时, 新 coordinator 会丢失本地防递归合同。该适配由两个定向测试覆盖, 属于保留本地功能的合并兼容, 不是上游 bug 修复。

## 4. 本地功能保留矩阵

| 本地能力 | 决策 | 代码/测试证据 | 复核说明 |
|---|---|---|---|
| RequestArchive / RequestIntercept | **保留** | `backend/internal/server/middleware/request_archive.go`; `request_intercept.go`; 对应 middleware/route tests | 网关 middleware 与 `/admin/request-intercept` 均保留。 |
| Prompt Metrics | **保留** | `backend/internal/service/promptmetrics/`; `frontend/src/views/admin/PromptMetricsView.vue` | 与上游 Prompt Audit 职责不同, ProviderSet、middleware、UI 和 shutdown 均保留。 |
| Prompt Risk / LLM judge | **组合** | `content_moderation.go`; `prompt_risk*.go`; `security_audit_helper_test.go`; `coordinator_legacy_test.go` | 既有规则/judge 作为 legacy moderation 接入新 coordinator, 防递归签名头完整传递。 |
| Token Analysis | **保留** | `token_analysis_*`; `frontend/src/views/admin/TokenAnalysisView.vue` | handler/service/repository/UI 与后台索引保留。 |
| 组织用量 | **保留** | `organization_usage_*`; `OrganizationUsageView.vue` | API、SQL 口径、导出与页面未被上游覆盖。 |
| 图片生成与支付 | **组合** | `ImageGenView.vue`; `image_generation_intent.go`; payment routes/services | 本地 ImageGen/支付保留, 同时采用上游 Grok media eligibility 与 explicit intent 增量。 |
| 用户并发 preset / `ConcurrencyCacheError` | **保留** | `user_concurrency_preset_*`; `concurrency_service.go`; 对应 tests | runner、缓存错误和 503 合同继续存在。 |
| OpenAI-compatible preset/options/cache usage | **保留** | `openaiCompatibleProviderPresets.ts`; `internal/pkg/apicompat/`; gateway usage tests | 本地 provider preset、options 过滤及 cache read/write 兼容未被替代。 |
| 默认 reasoning effort | **保留** | `backend/internal/config/config.go`; `openai_gateway_chat_completions.go`; 对应 tests | 配置和注入路径未与本轮上游重叠删除。 |
| user-platform quota flusher | **保留** | `user_platform_quota_flusher.go`; Wire cleanup; repository tests | dirty snapshot、批量落库和 shutdown 仍接入。 |

未发现可由 upstream 完整等价替代并安全移除的本地能力, 因此“上游替代”项为 0。

## 5. 上游 0.1.160 事实

| 领域 | 合入行为 |
|---|---|
| Prompt Audit | 默认关闭；支持 off / async audit / blocking；OpenAI-compatible Guard 节点、策略/group 范围、runtime、事件查询与筛选删除由管理端配置。 |
| 存储 | migration 181 新增 `prompt_audit_jobs` / `prompt_audit_events`; async 扫描原文在 Redis 最长 30 分钟。migration 182 进一步把未脱敏 `full_prompt` 持久化到 event, 上限 65,536 rune。 |
| Frontend | `/admin/prompt-audit` 位于 Security Audit 分组；页面包含 policy、endpoint pool、runtime、event workspace/detail 和筛选删除。 |
| Grok media | generation 调度引入账号资格 capability, scheduler cache 保留资格/billing snapshot, image reference 字段规范为上游 `url`。 |
| S3 step-up | `BackupView.vue` 保存 S3 配置接入既有 step-up TOTP 流。 |
| Frontend dependency | 显式加入 `@intlify/message-compiler@9.14.5`, 完整 locale compile suite 可在 pnpm strict linking 下收集。 |

## 6. 已披露且未修复的上游问题

| 项目 | 证据 | 本轮处置 |
|---|---|---|
| Prompt Audit Wire 源图缺接口绑定 | `securityaudit.ProviderSet` 提供 `NewPromptService`, 但没有 `wire.Bind(new(PromptAdminService), new(*PromptService))`; `go generate ./cmd/server` 报缺少 provider。 | 只记录；保留合并后的现有 `wire_gen.go`, 不修改 upstream ProviderSet。 |
| OpenSpec source-freeze patch 自带 whitespace | `openspec/.../source-freeze/aicodex-prompt-audit-tracked.patch` 在无过滤 `git diff --cached --check` 中报告 trailing whitespace / space-before-tab。 | 作为上游冻结证据原样保留；增量源码/文档使用排除该 artifact 的 diff check。 |
| Prompt 原文持久化改变早期设计 | migration 181 注释称 raw prompt 不进 PostgreSQL, 但 migration 182 与 repository 实际写入 event `full_prompt`。 | 以当前源码为准更新 wiki, 不改上游实现。 |

## 7. 验证结果

| 验证 | 结果 |
|---|---|
| Backend targeted | `cmd/server`, `internal/securityaudit`, `internal/server/routes`, `internal/handler` 通过。 |
| Backend unit | `go test -tags=unit -p 1 -count=1 ./...` 退出 0。 |
| Backend integration | `go test -tags=integration -p 1 -count=1 ./...` 退出 0。 |
| Backend lint | 全量 28 条本地 `main` 既有债务；`golangci-lint run --new-from-rev main ./...` 为 `0 issues`。 |
| Go modules | `go mod tidy -diff` 只建议删除既有 Wire CLI 传递校验和, 未修改 `go.mod/go.sum`。 |
| Wire generation | `go generate ./cmd/server` 按预期退出 1, 精确报缺少 `securityaudit.PromptAdminService` provider；按用户边界只披露, 不修复。 |
| Frontend targeted | Prompt Audit、layout、router 共 14 files / 94 tests 通过。 |
| Frontend full | lint/typecheck 退出 0；Vitest 194 files / 1330 tests 通过；build 979 modules, 退出 0。 |
| Embed build | 退出 0；产物 149,248,512 bytes。 |
| Git hard gate | `HEAD=main@5d5f157854b9`, `MERGE_HEAD=upstream/main@57914967cbb1`, merge base `c2c19a7cbe8`；143 files / `+20209 -118`。无 unstaged、untracked、unmerged、冲突标记或 `docs/features` 删除；排除上游 source-freeze patch 后 cached/HEAD diff check 均退出 0。 |

## 8. Commit 前状态

当前保持 `MERGE_HEAD=57914967cbb127ff715719c3879d881c10d75274`, 未执行 commit、push、PR 或部署。冲突决策、残余风险、测试与 staged diff 已具备审核条件, 等待用户决定是否授权创建 merge commit。
