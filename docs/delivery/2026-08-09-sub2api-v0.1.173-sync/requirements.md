# 需求说明

| 字段 | 内容 |
|---|---|
| 目标 | 基于本地 `main` 新建分支，将 `Wei-Shaw/sub2api main@48eb3766d2da817b171b45bb3036d42575e42b8f` 合并进来，解决合并冲突并完成文档与验证。 |
| 背景 | 同步上游 0.1.173，同时保留本项目在 `docs/features/` 所记录及对应源码实现的本地能力。 |
| 用户 / 受众 | 项目维护者、后续审核与发布人员。 |
| 提出人 | 用户 |
| 日期 | 2026-08-09 |

## 成功标准

| ID | 标准 | 需要的证据 |
|---|---|---|
| SC-1 | 工作分支准确基于本地及自有 GitHub `main` 的同一提交。 | 分支、远端 URL、提交 SHA 实时核对。 |
| SC-2 | 固定上游提交作为第二父进入待提交 merge，所有文本冲突均已解决。 | `MERGE_HEAD`、`git ls-files -u`、冲突标记扫描。 |
| SC-3 | 本地新增功能保持可用；与上游功能重叠时优先采用上游实现。 | 三方差异审计、冲突语义审查、`docs/features/` 保留检查。 |
| SC-4 | 不修复随上游引入的既有缺陷，只解决冲突及合并所必需的接口适配。 | 额外编辑审计与残余风险记录。 |
| SC-5 | 更新 `llm-wiki/wiki/` 与上游合并追加记录。 | 文档 diff、Wiki 图谱刷新与状态检查。 |
| SC-6 | 执行后端、前端、专项测试和构建，并如实区分通过、既有失败和环境阻塞。 | `test-review.md` 中的命令与结果。 |
| SC-7 | 在创建 commit 前停止并通知用户审核。 | 保留 `MERGE_HEAD`，不 commit、不 push、不建 PR。 |

## 范围

| 本次范围 | 不在本次范围 |
|---|---|
| 合并上游 `48eb3766...`；语义解决冲突；必要的测试桩签名适配；生成 Wire/Ent；更新 wiki、合并记录和交付记录；运行验证。 | 修复 migration 206/220 等上游既有问题；无关重构；部署；提交、推送或创建 PR。 |

## 约束

| 类型 | 约束 |
|---|---|
| 技术 | 以源码与现有测试为准；Wire/Ent 从源定义重新生成；migration 不重命名、不修改已发布文件。 |
| 产品 | `docs/features/` 本地功能完整保留；真正重叠的能力采用上游实现。 |
| 安全 / 数据 | 根级 Grok 别名不得绕过 RequestArchive/RequestIntercept、API Key、group gate；OAuth session 和敏感字段边界保持上游合同。 |
| 分支 / 发布 | 分支为 `feature/hy/10173_merge_sub2api_173`；提交前必须由用户审核。 |

## 假设

| ID | 假设 | 为什么安全 |
|---|---|---|
| A-1 | “我们自己的 GitHub”指 remote `github` 的 `Oracle0703/xyai`。 | remote URL 已实时确认；`origin` 实际为内网 Git 服务。 |
| A-2 | 初始明确执行指令构成实施授权，最终提交仍单独等待审核。 | 用户已经要求直接合并、测试，并明确指定提交前审核点。 |

## 待确认问题

| ID | 问题 | 不确认的影响 | 状态 |
|---|---|---|---|
| Q-1 | merge commit SHA 何时写入追加记录？ | SHA 在提交前不存在，无法在当前待提交快照中填写最终值。 | 待提交审核后补录最终 SHA。 |
