# 需求说明

| 字段 | 内容 |
|---|---|
| 目标 | 基于最新本地 `main` 新建分支，将 `Wei-Shaw/sub2api main@98d86915becae9fe9491a91ffc6defd5235c8d2b` 合并进来，解决合并冲突并完成文档与验证。 |
| 背景 | 同步上游 `0.2.4`，同时保留本项目在 `docs/features/` 所记录及对应源码实现的本地能力。 |
| 用户 / 受众 | 项目维护者、后续审核与发布人员。 |
| 日期 | 2026-09-09 |

## 成功标准

| ID | 标准 | 需要的证据 |
|---|---|---|
| SC-1 | 工作分支准确基于最新本地及自有 GitHub `main` 的同一提交。 | 分支、远端 URL、提交 SHA 实时核对。 |
| SC-2 | 固定上游提交作为第二父进入待提交 merge，所有文本冲突均已解决。 | `MERGE_HEAD`、`git ls-files -u`、冲突标记扫描。 |
| SC-3 | 本地新增功能保持可用；与上游功能重叠时采用上游实现。 | 三方差异审计、双方修改文件语义审查、`docs/features/` 保留检查。 |
| SC-4 | 不修复随上游引入的缺陷，只解决冲突及合并所必需的接口适配。 | 额外编辑审计与残余风险记录。 |
| SC-5 | 更新 `llm-wiki/wiki/` 与 append-only 上游合并记录。 | 文档 diff、Wiki 图谱刷新与状态检查。 |
| SC-6 | 执行后端、前端、专项测试和构建，并如实区分通过、既有失败、上游失败和环境阻塞。 | `test-review.md` 中的命令与结果。 |
| SC-7 | 在创建 commit 前停止并通知用户审核。 | 保留 `MERGE_HEAD`，不 commit、不 push、不建 PR。 |

## 范围

| 本次范围 | 不在本次范围 |
|---|---|
| 合并 exact SHA；语义审查自动合并和文本冲突；必要的生成物刷新；更新 wiki、合并记录和交付记录；运行验证。 | 修复目标提交中的上游缺陷；无关重构；部署；创建 commit、push 或 PR。 |

## 约束

| 类型 | 约束 |
|---|---|
| 技术 | 以源码与现有测试为准；生成文件只能从源定义刷新；只进行合并所必需的适配。 |
| 产品 | 24 个合并前 tracked `docs/features/` 文件及其本地功能必须保留；真正重叠的能力采用上游实现。 |
| 分支 / 发布 | 分支使用 `feature/hy/10204_merge_sub2api_204`；提交前必须由用户审核。 |
| 上游边界 | 固定 `98d86915becae9fe9491a91ffc6defd5235c8d2b`，不得用后续 upstream HEAD 替代。 |

## 假设

| ID | 假设 | 为什么安全 |
|---|---|---|
| A-1 | “最新本地 main”同时要求刷新并确认自有 GitHub `main`。 | `github` 指向 `Oracle0703/xyai`，刷新后两者均为 `f6bce5db`。 |
| A-2 | 用户初始指令构成创建分支、合并、文档和测试的实施授权。 | 用户已明确要求直接执行完整流程，且另行指定了 commit 前审核点。 |

## 待确认问题

| ID | 问题 | 当前处理 |
|---|---|---|
| Q-1 | merge commit SHA 在待审核快照中尚不存在。 | append-only 记录标记为“待用户审核后创建”；实际 commit 后再按规则补录真实 SHA。 |
