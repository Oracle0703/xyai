# 需求说明

| 字段 | 内容 |
|---|---|
| 目标 | 基于 0.1.151 分支创建新分支并合入 Wei-Shaw/sub2api 0.1.153，保留上游修改与本地功能，完成文档和测试 |
| 背景 | 本地仓库长期维护 RequestArchive、RequestIntercept、Token Analysis、图片生成、Prompt Risk、用户并发与兼容层扩展 |
| 用户 / 受众 | 仓库维护者、后续开发者与部署人员 |
| 提出人 | 当前仓库用户 |
| 日期 | 2026-07-13 |

## 成功标准

| ID | 标准 | 需要的证据 |
|---|---|---|
| SC-1 | 新分支以 151 版本为第一父基线 | merge commit 第一父为 `5e6e85568792` |
| SC-2 | 只合入上游 `55ed0ab0d`，不含后续提交 | merge commit 第二父、ancestor 检查和上游剩余提交列表 |
| SC-3 | 6 个冲突均按两侧语义解决 | 冲突文件清单、combined diff、聚焦测试和 review 报告 |
| SC-4 | 本地扩展和上游 153 能力同时保留 | 入口扫描、代码审查、前后端测试 |
| SC-5 | merge ledger、组件 README 与 llm-wiki 完整更新 | 文档 diff 和追加型 ledger 检查 |
| SC-6 | 全量 unit/integration、前端 lint/typecheck/test/build 与 embed build 完成 | 最终验证日志和退出码 |

## 范围

| 本次范围 | 不在本次范围 |
|---|---|
| 创建并维护 `feature/hy/10153_同步sub2api主线` | 合入 `7d239d62e` 或其后的上游提交 |
| 合入固定上游 0.1.153 SHA 并解决冲突 | 部署生产环境、重启服务或执行数据库操作 |
| 复核本地能力与上游新增行为 | 顺手重构与本次合并无关的历史代码 |
| 补齐 merge ledger、review、wiki 和组件文档 | 推送额外提交或创建 PR，除非用户另行要求 |
| 运行完整验证并报告历史基线问题 | 将 151 基线 lint 债务冒充为本次回归 |

## 约束

| 类型 | 约束 |
|---|---|
| 技术 | Windows 上 Go 测试使用 repo-local cache、fresh `GOTMPDIR`、`-p 1 -count=1` |
| 产品 | 上游新增能力和本地功能均需保留，不能整侧覆盖 |
| 安全 / 数据 | migration 只追加不修改；支付泄露接口删除和 WS 限制不得回退 |
| 分支 / 发布 | 分支名遵循 `feature/hy/10xxx_XXX`；固定上游 SHA；默认不部署 |

## 假设

| ID | 假设 | 为什么安全 |
|---|---|---|
| A-1 | `5e6e85568792` 是用户所指 151 基线 | 该提交位于 151 同步分支，且是 merge commit 第一父 |
| A-2 | 已有 merge commit 不重写 | 它的父链符合用户最新边界，重写会破坏已推送拓扑 |
| A-3 | `go mod tidy` 删除的 8 行校验项可独立提交 | `go mod tidy -diff` 为 0，说明当前依赖图稳定 |

## 已决策问题

| ID | 问题 | 不确认的影响 | 状态 |
|---|---|---|---|
| Q-1 | 是否推送文档收口提交 | 不影响本地完成，但远端分支暂不会包含后续文档提交 | 已决策：本轮不推送，等待用户明确要求 |
